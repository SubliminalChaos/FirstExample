package me.waqe.hariri.classes

data class DataClasses(val id: Int = 1) {
    // toString()  formatted output, rather than pointer
    // hashCode()
    // equals()    one-by-one comparison of properties/values
    // copy()      " + can change properties if desired!

    // can override toString() and hashCode() if desired

}
